<%= form_for(student) do |f| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_field :name, placeholder: "Name", class: "form-control", autofocus: true %>
  </div>

  <div class="form-group">
    <%= f.text_field :year, placeholder: "Year", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.number_field :score, placeholder: "Score", class: "form-control" %> 

  </div>

  <div class="form-group">
    <%= f.text_field :grade, placeholder: "Grade", class: "form-control" %>
  </div>

  <div class="actions">
<%= f.submit "Make It Happen!", class: "btn btn-success" %>
  </div>
<% end %>
